// const[keyWords, setKeyWords] = useState();
// const[rhymingWords, setRhymingWords] = useState();

// keyWords === ['dog', 'cat,]


// loop through keyWords and call api for each item of the array and update state of rhymingWords

// setRhymingWords([...rhymingWords, (call return from 'dog')])
// setRhymingWords([...rhymingWords, (call return from 'cat')])